% =====================================================================
%  Condensed crystal graphs (graph-construction figure) — standalone WRAPPER
% ---------------------------------------------------------------------
%  The drawing lives in graph_construction_fig.tex (a preamble-free
%  fragment).  This wrapper compiles a cropped preview PDF:
%
%     pdflatex graph_construction.tex          (Overleaf: just hit Recompile)
%
%  The main paper should NOT include this file — it should \input the
%  fragment inside a figure* environment; see the usage note and the
%  suggested caption at the top of graph_construction_fig.tex.
%  The Row-1 structure motifs inside the fragment are generated by
%  gen_motif3d.py (this directory).
%  Needs: tikz + the libraries below (texlive-pictures / texlive-latex-extra).
% =====================================================================
\documentclass[border=10pt]{standalone}
\usepackage{amsmath}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc,backgrounds}

\begin{document}
\input{graph_construction_fig}
\end{document}
