/model_data/

# All database data files (CIFs, graphs, pickles, CSVs) live under
# database/datafiles/ and are NOT versioned in git. Scripts live in database/.
/database/datafiles/

# Python caches
__pycache__/
*.pyc

# Run outputs / checkpoints / logs
*.pth.tar
/logs/
# Unanchored so the models/ reorg keeps the same run-outputs ignored as the old CNN/ tree.
model_data/
mpnn_result*
test_result_epoch_log.csv

# LaTeX build artifacts (docs/figures)
docs/figures/latex/*.aux
docs/figures/latex/*.log

# Local scratch with credentials — never commit
/test.py
